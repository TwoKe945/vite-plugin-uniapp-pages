import { defineConfig } from 'bumpp'

export default defineConfig({
  files: [
    'package.json',
    'packages/core/package.json',
    'packages/types/package.json',
    'playground/split-pages-config/package.json',
  ],
})
